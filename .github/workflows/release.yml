name: Release
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install frontend dependencies
        run: pnpm install

      - name: Build and Release Tauri App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PASSWORD: ${{ secrets.TAURI_SIGNING_PASSWORD }}
        with:
          tagName: ${{ github.ref_name || 'v1.0.3' }}
          releaseName: "Smart Cleaner ${{ github.ref_name || 'v1.0.3' }}"
          releaseBody: |
            ### 📥 安装说明 (macOS)
            由于此应用未经过苹果官方签名，初次运行可能会提示“无法验证开发者”。
            请在终端执行以下命令解除安全限制：

            ```bash
            sudo xattr -rd com.apple.quarantine "/Applications/Smart Cleaner.app"
            ```
            执行后即可正常打开。
          releaseDraft: false
          prerelease: false
          args: --target aarch64-apple-darwin --target x86_64-apple-darwin
