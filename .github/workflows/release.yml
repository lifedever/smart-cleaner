name: Release
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install frontend dependencies
        run: pnpm install

      - name: Build macOS Assets
        run: node scripts/release.mjs
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: "sc2026"

      - name: Create and Upload Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag_name="${{ github.ref_name || 'v1.1.7' }}"
          # Create release if it doesn't exist, or update it
          gh release create "$tag_name" \
            --title "Smart Cleaner $tag_name" \
            --notes "### 📥 安装说明 (macOS)
            由于此应用未经过苹果官方签名，初次运行可能会提示“无法验证开发者”。
            请在终端执行以下命令解除安全限制：
            
            \`\`\`bash
            sudo xattr -rd com.apple.quarantine \"/Applications/Smart Cleaner.app\"
            \`\`\`
            执行后即可正常打开。" \
            || echo "Release already exists, uploading assets..."

          # Find and upload all DMGs and updater tarballs (renamed with arch suffix)
          find src-tauri/target -name "*.dmg" -exec gh release upload "$tag_name" {} --clobber \;
          find src-tauri/target -name "*_aarch64.app.tar.gz" -exec gh release upload "$tag_name" {} --clobber \;
          find src-tauri/target -name "*_x86_64.app.tar.gz" -exec gh release upload "$tag_name" {} --clobber \;

          # Upload the consolidated updater JSON
          gh release upload "$tag_name" latest.json --clobber

  release-windows:
    permissions:
      contents: write
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install frontend dependencies
        run: pnpm install

      - name: Build Windows Assets
        run: pnpm tauri build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: "sc2026"

      - name: Upload Release Assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          tag_name="${{ github.ref_name || 'v1.1.7' }}"
          # Get all generated setups (msi and exe)
          for file in src-tauri/target/release/bundle/msi/*.msi src-tauri/target/release/bundle/nsis/*.exe; do
            if [ -f "$file" ]; then
              gh release upload "$tag_name" "$file" --clobber || true
            fi
          done
